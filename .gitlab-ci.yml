stages:
  - dependencies
  - pipeline
  - test

build-code-job:
  stage: build
  script:

install-dependencies:
    stage: dependencies
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8.6'
    - name: Install dependencies
      run: python -m pip install --upgrade pip setuptools whee

test-code-job:
  stage: test
  script:
    - echo "Ejecutamos los tests"
    - python test.py

analize-pslint:
    stage: pipeline
    script: pip install pylint --upgrade
            pylint test.py
    
default:
  image: python:3.8.6-alpine3.12